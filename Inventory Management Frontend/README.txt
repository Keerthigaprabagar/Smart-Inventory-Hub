Inventory Frontend (Angular 20) — Quick Start

1) Unzip this folder.
2) In the project root run:
   npm install
3) Start the dev server:
   npx ng serve --open
4) The app expects the backend API at http://localhost:5160 (update src/app/services/api.service.ts if different).
